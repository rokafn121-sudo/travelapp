# Travel Budget Tracker - Premium Upgrade Proposals ✨

이 문서는 "영현이의 여행" 앱을 단순한 가계부 기능에서 **"앱스토어/플레이스토어에 출시해도 손색없는 고급형(Premium) 여행 경비 관리 앱"**으로 탈바꿈하기 위한 전문가 관점의 개선 아이디어입니다.

---

## 🎨 1. UI/UX 디자인 고급화 (Premium Aesthetics)

### A. 최신 웹 트렌드 적용 (Glassmorphism & 미세 애니메이션)
* **카드 뷰 개선**: 현재 단순한 하얀색 배경 대신, 은은한 반투명 블러 효과(Glassmorphism)와 그라데이션을 적용하여 화면이 겹칠 때 입체감과 깊이감을 줍니다.
* **Hover & Active 효과 강화**: 지출 내역이나 방 목록을 탭/호버 할 때 미세하게 커지는 팝업 애니메이션과 부드러운 전환 효과(Transition)를 주어 사용성을 높입니다.
* **커스텀 폰트(Typography)**: 기본 시스템 폰트에서 `Pretendard`, `Toss Face(토스 페이스)` 등의 깔끔하고 모던한 폰트와 이모지를 로드하여 고급스러운 브랜드 아이덴티티를 구축합니다.

### B. 상태별 디자인 (Empty States & Feedback)
* **Empty State 일러스트**: 지출 내역이나 방이 하나도 없을 때 단순히 "내역이 없습니다"라는 텍스트 대신, 귀여운 비행기나 지갑 모양의 일러스트 SVG를 배치해 감성적인 경험을 줍니다.
* **스켈레톤(Skeleton) 로딩**: 페이지를 이동하거나 환율을 로딩할 때, 흰 화면이나 스피너 대신 스켈레톤 UI를 띄워 로딩 체감 시간을 줄입니다.

---

## 🛠️ 2. 핵심 기능(Feature) 고도화

### A. 영수증 이미지 첨부 기능 (Advanced Input)
* **사진 업로드**: 지출을 기록할 때, 맛있게 먹은 음식 사진이나 **영수증 사진**을 함께 업로드(카메라 연동) 할 수 있도록 합니다. (스트림릿의 `st.file_uploader` 또는 `st.camera_input` 활용)
* 지출 내역 리스트에서 썸네일로 해당 사진을 볼 수 있어 여행 일기장 역할도 겸하게 됩니다.

### B. 카테고리별 예산(Budget limits) 설정 및 경고
* 전체 여행 예산(Total Budget) 뿐만 아니라, "식비 40%", "쇼핑 20%" 와 같이 **카테고리별 세부 예산**을 설정할 수 있게 합니다.
* 특정 카테고리가 예산에 도달하면 카드 색상이 빨간색으로 변하거나 알림이 뜨는 등 직관적인 피드백을 줍니다.

### C. 여행 요약 리포트 내보내기 (Export & Share)
* 여행이 끝난 후, 클릭 한 번으로 모든 지출 내역과 통계 차트를 예쁜 **PDF 형식이나 깔끔한 Excel 템플릿**으로 다운로드하여 동행자(가족, 친구)와 완벽하게 공유(정산)할 수 있는 기능을 추가합니다.

---

## 📊 3. 데이터 시각화 (Data Visualization)

### A. 일자별 타임라인 뷰 (Interactive Timeline)
* 파이 차트(비율)뿐만 아니라, **"여행 1일차, 2일차..." 날짜별 지출 흐름을 보여주는 누적 막대그래프(Bar chart)**를 추가합니다. 
* 꺾은선 그래프로 "예산 소진율(Burn Rate)"을 시각적으로 보여주어, 어느 날 돈을 제일 많이 썼는지 한눈에 파악하게 합니다.

### B. 다중 통화(Multi-Currency) 혼합 차트
* 현지 화폐(예: JPY) 결제 건과 원화(KRW) 결제 건(항공권 등)이 섞여 있을 때, 어떤 통화로 얼마를 지불했는지를 분리해서 보여주는 대시보드 위젯을 제공합니다.

---

## ⚙️ 4. 시스템 아키텍처 및 안정성 (Architecture)

### A. 데이터베이스(DB) 마이그레이션
* 현재 `CSV` 및 `JSON` 파일 기반 저장 방식은 동시 접속자가 많거나 지출 내역이 수천 건으로 쌓이면 덮어쓰기 위험과 성능 저하가 발생합니다.
* 보다 견고한 `SQLite` 또는 `PostgreSQL`(Cloud)로 데이터 계층을 마이그레이션하여 빠르고 안전하게 데이터를 렌더링하도록 구조를 변경합니다.

### B. 페이지 라우팅 분리 (Componentization)
* 단일 파일(`app.py`) 로직이 너무 커지는 것을 방지하기 위해 스트림릿의 **Multi-page apps** 구조로 분할하거나, `components/`, `views/` 디렉터리를 만들어 코드를 모듈화합니다. (유지보수 및 확장이 매우 용이해짐)
